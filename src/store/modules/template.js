import http from '@/utils/http';const app = {  namespaced: true,  state: {    templateList: [],    currentTemplate: {},  },  mutations: {    setCurrentTemplate(state, data) {      Object.assign(state, {currentTemplate: data});    }  },  actions: {    async getTemplate({commit}, id) {      const res = await http.get(`/templates/${id}`);      commit('setCurrentTemplate', res.data.data);    },    async updateTemplate({commit}, {value, id, temp,}) {      if (temp == "project_item") {        await http.put(`/templates/${temp}/${id}`, {"title": value });      } else if (temp == "template_input") {        await http.put(`/templates/${temp}/${id}`, {"title": value });      } else if (temp == "project") {        await http.put(`/templates/${temp}/${id}`, {"title": value });      } else if (temp == "score") {        await http.put(`/templates/project_item/${id}`, {"score": value });      }    }  },};export default app;